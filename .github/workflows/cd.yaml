name: Main Pipeline

on:
  push:
    branches: [ master ]

jobs:
  # integration-testing:
  #   uses: ./.github/workflows/cd-integration-testing.yaml

  # k6-testing:
  #   needs: integration-testing
  #   uses: ./.github/workflows/cd-k6-testing.yaml

  deploy-ke-aws:
    # needs: k6-testing
    uses: ./.github/workflows/cd-upload-apps.yaml
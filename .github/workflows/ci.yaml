name: Main Pipeline

on:
  push:
    branches: [ dev ]

jobs:
  integration-testing:
    uses: ./.github/workflows/ci-integration-testing.yaml

  k6-testing:
    needs: integration-testing
    uses: ./.github/workflows/ci-k6-testing.yaml